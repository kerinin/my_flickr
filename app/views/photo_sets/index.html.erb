<h1>Listing photo_sets</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @photo_sets.each do |photo_set| %>
  <tr>
    <td><%= link_to 'Show', photo_set %></td>
    <td><%= link_to 'Edit', edit_photo_set_path(photo_set) %></td>
    <td><%= link_to 'Destroy', photo_set, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Photo set', new_photo_set_path %>
